# AI API Configuration
# 设置 VITE_USE_LOCAL_SKILL=true 启用本地 skill 模式（无需后端服务器）
# 设置 VITE_USE_LOCAL_SKILL=false 则使用后端 API
VITE_USE_LOCAL_SKILL=true

# 后端 API URL（仅在 VITE_USE_LOCAL_SKILL=false 时使用）
VITE_AI_API_URL=/api/ai

# Gemini API Key（如需要使用 Gemini 功能）
GEMINI_API_KEY=your_gemini_api_key_here
