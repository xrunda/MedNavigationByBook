import { View, Text } from '@tarojs/components';
import './index.scss';

const Page = () => {
  return (
    <View className="page-container">
      <Text className="page-title">页面开发中...</Text>
    </View>
  );
};

export default Page;
